# MarkPen for VSCode

Markdown editor with live preview. No separate tab.

## Pre market release issues

1. README + icon + .vscodeignore + package.json — publish
2. Test Windows/Mac/Linux — publish se pehle zaroori
3. make seclect all codeblock selector specific to the editing code block. not the preview.
4. Add Table, List Heading prefixes to the command texts.
5. Shorten Markpen menu options to - toogle editor, toggle statusbar buttons, Hotkeys, Paste Image
6. Add ease smooth animation to IR markers showing up
7. Remove not used config settings.
